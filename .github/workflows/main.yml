name: Build unsigned IPA

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Build unsigned archive
        run: |
          xcodebuild archive \
            -project WorkPlot/WorkPlot.xcodeproj \
            -scheme WorkPlot \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$RUNNER_TEMP/WorkPlot.xcarchive" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Package unsigned IPA
        run: |
          mkdir -p "$RUNNER_TEMP/Payload"
          cp -R \
            "$RUNNER_TEMP/WorkPlot.xcarchive/Products/Applications/WorkPlot.app" \
            "$RUNNER_TEMP/Payload/"
          cd "$RUNNER_TEMP"
          zip -qry WorkPlot.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: WorkPlot-IPA
          path: ${{ runner.temp }}/WorkPlot.ipa

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download built IPA
        uses: actions/download-artifact@v4
        with:
          name: WorkPlot-IPA

      - name: Attach IPA to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: WorkPlot.ipa
          generate_release_notes: true

  latest-release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          fetch-depth: 10

      - name: Download built IPA
        uses: actions/download-artifact@v4
        with:
          name: WorkPlot-IPA

      - name: Prepare dynamic release metadata
        id: meta
        env:
          SHA_FULL: ${{ github.sha }}
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          SHORT_SHA="${SHA_FULL::7}"
          echo "title=WorkPlot dev 1.0.${BUILD_NUMBER} (${SHORT_SHA})" >> "$GITHUB_OUTPUT"
          BODY_FILE="${RUNNER_TEMP}/release-body.md"
          {
            echo "Build otomatis dari commit \`${SHA_FULL}\` di branch main."
            echo ""
            echo "### Commit terbaru"
            echo ""
            echo '```'
            git log --oneline -5 --no-decorate
            echo '```'
            echo ""
            echo "Dibangun: $(date -u '+%Y-%m-%d %H:%M UTC')"
            echo ""
            echo "> ⚠️ Ini prerelease dev (unsigned IPA) yang selalu ditimpa build berikutnya."
            echo "> Untuk rilis stabil, gunakan tag \`v*\`."
          } > "$BODY_FILE"
          echo "body_path=${BODY_FILE}" >> "$GITHUB_OUTPUT"

      - name: Delete previous rolling release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest --cleanup-tag -y -R "$GITHUB_REPOSITORY" || true

      - name: Attach IPA to rolling 'latest' release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: ${{ steps.meta.outputs.title }}
          body_path: ${{ steps.meta.outputs.body_path }}
          files: WorkPlot.ipa
          prerelease: true
          make_latest: false
          generate_release_notes: false
